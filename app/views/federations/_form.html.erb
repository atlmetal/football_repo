<%= form_with(model: federation) do |form| %>
  <% if federation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(federation.errors.count, "error") %> prohibited this federation from being saved:</h2>

      <ul>
        <% federation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_area :name %>
  </div>

  <div>
    <%= form.label :foundation_date, style: "display: block" %>
    <%= form.date_field :foundation_date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
